# Character-Cookbook
## Libraries

Contrary to the "Bookshelf" folder, this folder will store the (c++) libraries that the application itself will use in order to be functionnal.

This will be where our classes & functions will be stored, with (maybe) some exceptions.